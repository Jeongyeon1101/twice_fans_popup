<h2>商品詳細</h2>
<%= image_tag @item.get_image(300,200) %>
<table>
  <tr>
    <td><strong>商品名</strong></td>
    <td><%= @item.name %></td>
  </tr>
  <tr>
    <td><strong>商品説明</strong></td>
    <td><%= @item.introduction %></td>
  </tr>
  <tr>
    <td><strong>ジャンル</strong></td>
    <td><%= @item.genre.name %></td>
  </tr>
  <tr>
    <td><strong>税込価格(税抜価格)</strong></td>
    <!-- to_s: 文字列以外のオブジェクトを文字列に変換することができる -->
    <!-- delimited: 数値を3桁区切りで表示することができる -->
    <td>¥<%= @item.with_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)</td>
  </tr>
  <tr>
    <td><strong>販売ステータス</strong></td>
    <% if @item.is_active? %>
      <td><strong>販売中</strong></td>
    <% else %>
      <td><strong>販売停止中</strong></td>
    <% end %>
  </tr>
</table>
<%= link_to "編集する", edit_admin_item_path(@item) %>